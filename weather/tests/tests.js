'use strict';

const chai = require('chai');
chai.should();

const weather = require('../index.js');

//asynchronous code
describe('Weather', () => {
	it('should get the weather', (done) => {
	});
});


/**
 * describe('nameofthefunction', function(){
 * it('should done this', function(done){
 * 	set the variable
 *	test your function
 *  });
 * });
**/